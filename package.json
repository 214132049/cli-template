{
  "name": "{{ name }}",
  "version": "{{ version }}",
  "description": "{{ description }}",
  "main": "app.js",
  "scripts": {
    "test": "node run dev"
  },
  {{#if needHomePage }}
  "homepage": "{{ homepage }}",
  {{/if}}
  "author": "{{ author }}",
  "license": "ISC",
  "dependencies": {
    "vue": "^2.4.4"
  }
}
